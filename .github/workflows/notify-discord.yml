name: "🔕 Notify Discord (Disabled)"

on:
  workflow_run:
    workflows:
      - "🧪 Check Backend (Supabase + Build)"
      - "🧠 Design QA Check"
      - "🚀 Redeploy Vercel"
    types:
      - completed

jobs:
  notify_disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Skip notification
        run: echo "Discord notifications are disabled for now."
