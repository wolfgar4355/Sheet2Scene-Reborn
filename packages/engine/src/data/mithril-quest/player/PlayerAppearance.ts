// @ts-nocheck
export interface PlayerAppearance {
  poseKey?: string;        // lié à alignements-poses.ts
  iconKey?: string;        // lié à alignements-icons.ts
  hairColor?: string;
  eyeColor?: string;
  skinTone?: string;
  theme?: string;          // fée, ombre, arcane, runique, dragon, abyssal…
}
