{
  "name": "sheet2scene",
  "version": "1.0.0",
  "private": true,
  "packageManager": "npm@10.8.0",
  "type": "module",

  "engines": {
    "node": ">=20.10.0"
  },

  "workspaces": [
    "apps/*",
    "packages/*"
  ],

  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "lint": "turbo lint",
    "clean": "turbo clean",
    "generate": "turbo generate",
    "typecheck": "turbo typecheck"
  },

  "devDependencies": {
    "turbo": "^2.1.0",
    "typescript": "^5.4.0",
    "lint-staged": "^15.2.0"
  }
}
