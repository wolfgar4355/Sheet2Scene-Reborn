# GodinVerse — Hub de projet

Ce dépôt regroupe l’app **Next.js** (Sheet2Scene), la config **PM2** (prod), et l’orchestration des **agents IA** (Supabase).

## 🎯 Vision
- Offrir une application web modulaire.
- Coop entre 4 pôles : **Technique (grp-tech)**, **Sécurité & Conformité (grp-sec)**, **Performance & Release (grp-perf)**, **Communauté (grp-com)**.
- Les tâches sont déclarées dans `public.ai_tasks` (Supabase) et vues par pôle.

## 🧱 Architecture rapide
- **Frontend**: Next.js 14 (`/apps/web`)
- **Orchestration**: Supabase (Postgres + RLS) — tables `ai_groups`, `ai_agents`, `ai_group_members`, `ai_tasks`
- **Process manager**: PM2 (prod)
- **Logs**: `/var/log/pm2/*.log`

## 🔐 Variables d’environnement
Créer `.env.local` pour le dev :

## 🔗 Lien base de tâches

Les tâches et groupes sont gérés dans Supabase :

- **Table groupes** → `public.ai_groups`
- **Table tâches** → `public.ai_tasks`
- **Table agents** → `public.ai_agents`
- **Vues SQL sauvegardées** → `dash_summary_*`
